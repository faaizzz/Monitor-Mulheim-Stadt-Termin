# Copy to .env for local `npm run dev`. For Docker builds, these are passed
# as build args in docker-compose.yml instead (Vite inlines them at build
# time, so changing them requires rebuilding the dashboard image).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
