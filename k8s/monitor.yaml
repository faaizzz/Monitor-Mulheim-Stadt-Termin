apiVersion: apps/v1
kind: Deployment
metadata:
  name: mhtermin-monitor
  namespace: mhtermin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mhtermin-monitor
  template:
    metadata:
      labels:
        app: mhtermin-monitor
    spec:
      imagePullSecrets:
        - name: ghcr-credentials
      containers:
        - name: monitor
          image: ghcr.io/faaizzz/mhtermin-worker:latest
          imagePullPolicy: Always
          command: ['npx', 'playwright', 'test', 'tests/notify-monitor.spec.ts']
          envFrom:
            - secretRef:
                name: mhtermin-secrets
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 768Mi
